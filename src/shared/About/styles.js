const styles = theme => ({
  block: {
    padding: 15,
    marginBottom: 15,
  },
  blockText: {
    margin: '15px 0',
  },
  skill: {
    marginRight: theme.spacing.unit,
    marginBottom: theme.spacing.unit,
  },
});

export default styles;
